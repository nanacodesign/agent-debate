.DS_Store
node_modules/
.playwright-mcp/

# Local agent/editor config and preview launch configs.
.claude/

# Local debate output and private per-machine settings.
runs/*.md
!runs/.gitkeep
agent-debate.config.json
.env

# Internal agent instructions, local experiments, and working notes
# (kept local, not published).
AGENTS.md
experiments/
scratch/

# Local macOS launcher bundle generated for one machine.
macos/Agent Debate.app/

# Local screenshots should be regenerated without private paths before publishing.
*.png
!public/assets/*.png
